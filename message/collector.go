package message

import "github.com/lovoo/goka"

func New(group goka.Group, stream string, messageType goka.Codec) {
	// g := goka.DefineGroup(group, goka.Input(goka.Stream(stream), messageType, ))
}
